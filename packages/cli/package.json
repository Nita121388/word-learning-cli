{
  "name": "@word-learning/cli",
  "version": "0.1.1",
  "type": "module",
  "bin": {
    "wordcli": "./dist/main.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/main.ts",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@word-learning/core": "workspace:*",
    "commander": "^14.0.2",
    "zod": "^4.1.13"
  }
}
